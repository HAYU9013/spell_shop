# 咒語店 — 待完成清單

> 盤點時間：2026-03-21
> 結論：**核心邏輯已完備，剩下的工作幾乎全是 UI 與場景組裝。**

---

## 現況總覽

| 系統 | 狀態 |
|------|------|
| GameDefinitions、環境、符文、卷軸、遺物、顧客、牌庫、工作台 | ✅ |
| ScrollProcessor（6 種修飾規則） | ✅ |
| GameManager（Game Over / Level Clear） | ✅ |
| GameFacade（Read / Action / Event API） | ✅ |
| GameFacadeSnapshots（WorkbenchSnapshot / CustomerSnapshot / RelicSnapshot） | ✅ |
| RewardManager（Score / Rune / Scroll） | ✅ |
| TurnManager + 全 8 Phase 接線（含 Phase 5） | ✅ |
| Editor 資產建立腳本（4 支） | ✅ |

---

## 🔴 必要（跑不起來）

### 1. UI 腳本（一行都還沒有）

整個 `Assets/Scripts/` 找不到任何 UI 腳本，需要全部從頭撰寫：

| 腳本 | 顯示 / 負責內容 |
|------|----------------|
| `EnvironmentPanel.cs` | 亮度、水分、溫度數值 |
| `ScorePanel.cs` | 業績數值 + DOTween 跳字 |
| `HandPanel.cs` | 手牌列表，動態生成 RuneCard |
| `RuneCardUI.cs` | 單張符文卡牌，點擊放入工作台 |
| `WorkbenchPanel.cs` | 卷軸選擇按鈕列 + 符文槽位 + 送出鈕 + 預覽 delta |
| `CustomerPanel.cs` | 顧客立繪、需求進度文字、耐心進度條 |
| `RelicPanel.cs` | 遺物列表容器，動態生成 RelicIconUI |
| `RelicIconUI.cs` | 單個遺物圖示，滿意/不滿意顏色、計數文字、Tooltip |
| `GameOverPanel.cs` | 最終業績、通過回合數、「回主選單」按鈕 |
| `LevelClearPanel.cs` | 通關訊息、「回主選單」按鈕 |

**串接方式**：全部透過 `GameFacade.Instance`，不直接引用任何 Manager。
詳細事件與 API 見 `Plan/UI_entry_point.md`。

---

### 2. 場景組裝（GameScene.unity 現為空場景）

- [ ] 建立以下 GameObject 並掛上對應 MonoBehaviour：
  - `GameManager`（掛 `GameManager` + `GameFacade`）
  - `TurnManager`
  - `EnvironmentManager`
  - `DeckManager`
  - `ScrollInventory`
  - `WorkbenchManager`
  - `RelicManager`
  - `CustomerManager`
- [ ] 建立 Canvas（Screen Space - Overlay），擺放所有 UI Panel Prefab
- [ ] Inspector 串接：
  - `GameManager._turnManager` → 拖入 TurnManager
  - `GameManager._initialDeck` → 指派符文 SO 資產（水珠×3、小燭×3、蒸氣×2）
  - `GameManager._initialScrolls` → 指派卷軸 SO 資產（基礎卷軸×3）
  - `GameManager._initialRelics` → 指派遺物 SO 資產（日晷）
  - `GameManager._levelQueue` → 指派顧客 SO 資產（5 位，依序）
  - `GameManager._endlessPool` → 指派全部顧客 SO 資產
  - `WorkbenchManager` 的 `_turnManager` → 拖入 TurnManager

---

### 3. ScriptableObject 資產尚未建立

Editor 腳本已完成，但 `Assets/Data/` 中的 `.asset` 實際資產還未生成：

- [ ] 執行 `SpellShop → Create Default Runes`（生成 6 張符文）
- [ ] 執行 `SpellShop → Create Default Scrolls`（生成 3 張卷軸）
- [ ] 執行 `SpellShop → Create Default Relics`（生成 2 個遺物）
- [ ] 執行 `SpellShop → Create Default Customers`（生成 5 位顧客）

生成後再回到步驟 2 的 Inspector 指派。

---

## 🟠 次要（能跑但體驗缺失）

### 4. UI Prefabs 尚未製作

| Prefab | 用途 | 動態生成者 |
|--------|------|-----------|
| `RuneCard` | 手牌中的符文卡 | `HandPanel` |
| `RequirementRow` | 顧客面板中的需求列 | `CustomerPanel` |
| `RelicIcon` | 遺物面板中的圖示 | `RelicPanel` |
| `SlotUI` | 工作台符文槽 | `WorkbenchPanel` |
| `FloatingText` | 業績 +N / -N 浮字演出 | `ScorePanel` |

---

### 5. 主選單場景缺失

- [ ] 建立 `MainMenu.unity`
- [ ] 主選單 Controller 腳本（`MainMenuController.cs`）
  - 「關卡模式」按鈕 → `LoadScene("GameScene")` + 設定 `GameMode.Level`
  - 「無限模式」按鈕 → `LoadScene("GameScene")` + 設定 `GameMode.Endless`
- [ ] Build Settings 加入兩個場景（MainMenu index 0，GameScene index 1）

---

### 6. Relic 獎勵接受 / 拒絕 UI 未實作

- `RewardManager` 中 `RewardType.Relic` 有 `// TODO` 標記
- 需要一個確認 Panel：顯示遺物資訊，玩家選擇「接受」或「拒絕」
- [ ] `RelicRewardPanel.cs`（含接受/拒絕按鈕）
- [ ] `GameFacade.OnRelicRewardOffered` 事件（觸發 Panel 顯示）

---

## 🟡 美術（需 Art 端配合）

| 項目 | 數量 | 說明 |
|------|------|------|
| 背景底圖（店內場景） | 1 | |
| 環境圖示（亮度 / 水分 / 溫度 / 業績） | 4 | |
| 符文圖示 | 6 | 水珠、小燭、寒冰、黑霧、蒸氣、太陽核 |
| 顧客立繪 | 5 | 落湯雞旅人、感冒農夫、夜盲商人、冰魔法師、養花老人 |
| 遺物圖示 | 2 | 日晷、苔蘚石 |
| Game Over 畫面美術 | 1 | |
| Level Clear 畫面美術 | 1 | |
| 符文卡牌框架 | 1 | |
| 空槽 / 已放入 槽位圖示 | 2 | |

---

## 🟢 潤色（MVP 跑通後）

### DOTween 視覺效果

- [ ] `ScorePanel`：業績變動跳字（正值綠色 +N，負值紅色 -N）
- [ ] 極端事件觸發時螢幕邊緣閃紅（`DOTween` sequence）
- [ ] 送出按鈕點擊縮放動畫
- [ ] 符文卡牌 Hover 高亮（Scale 微放大）
- [ ] 顧客離開（滿足 / 憤怒）表情圖示切換

### 音效（有素材再做，否則跳過）

- [ ] 送出卷軸音效
- [ ] 顧客離開音效（滿意 / 憤怒 各一）

### Build

- [ ] 確認 Build Settings（Windows / WebGL）
- [ ] 打包並測試 Build 版本可正常啟動
- [ ] 準備 Gamejam 說明文字（一段話介紹玩法）

---

## 關鍵路徑（建議執行順序）

```
① 跑 Editor 腳本 → 生成 SO 資產（Runes / Scrolls / Relics / Customers）
        ↓
② 場景組裝 GameScene.unity
   → 放入所有 Manager GameObject
   → Inspector 指派 SO 資產與 Manager 引用
        ↓
③ 製作 UI Prefabs（RuneCard / RequirementRow / RelicIcon / SlotUI / FloatingText）
        ↓
④ 撰寫 6 個 Panel 腳本 + GameOverPanel + LevelClearPanel
   （全部透過 GameFacade，參考 UI_entry_point.md）
        ↓
⑤ 整跑一局驗收（關卡模式 + 無限模式各一局）
        ↓
⑥ 主選單場景
        ↓
⑦ 潤色（DOTween / 音效）+ Build
```

---

## 驗收標準（MVP 最低要求）

- [ ] 能抽到手牌
- [ ] 能選卷軸 + 放符文 + 點送出
- [ ] 環境數值正確更新
- [ ] 三種顧客需求類型（絕對值、相對變化、穩定性）正確判定
- [ ] 遺物滿意/不滿意效果正確，不滿意計數正確遞增/歸零
- [ ] 業績會增減
- [ ] 業績 = 0 → Game Over 畫面出現
- [ ] 關卡模式隊列清空 → Level Clear 畫面出現
- [ ] 無限模式顧客持續生成直到 Game Over
